"use strict";
//Hemos creado el objeto de forma literal en register.js pero ahora lo creamos con clases

class User {
    constructor(name, lastName, email, password) {
        this.name = name,
        this.lastName = lastName,
        this.email = email,
        this.password = password
    }

}